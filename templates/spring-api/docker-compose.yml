# templates/spring-api/docker-compose.yml
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: app_db
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: secret
    ports: ["5432:5432"]
    volumes: ["pg/var/lib/postgresql/data"]
volumes:
  pg
